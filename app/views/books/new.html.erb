<!--usersのshowページにこのページを記述した-->
<h1>New book</h1>

<!--エラーメッセージを記述-->
<% if @book.errors.any? %>
 <%= @book.errors prohibited this obj from being saved: %>
 <ul>
 <% @book.errors.full_messages.each do |message| %>
 <li><%= message %></il>
 <% end %>
 </ul>
<% end %>

<%= form_with model: @book do |f| %>
 <h4>Title</h4>
 <%= f.text_field :title %>
 <h4>Opinion</h4>
 <%= f.text_area :body %>
 <br>
 <%= f.submit 'Create Book' %>
<% end %>